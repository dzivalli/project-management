%h3
  Зарегистрированые компании
  = link_to new_company_path, type: 'button', class: 'btn btn-blue btn-icon icon-left pull-right get-modal' do
    Новая компания
    %i.entypo-plus
%br
%table.pm-table.table.table-bordered.datatable
  %thead
    %tr
      %th Имя компании
      %th Контакты
      %th Основной контакт
      %th Email
      %th Параметры
  %tbody
    - @companies.each do |company|
      %tr
        %td
          = link_to company_path(company), class: 'text-info' do
            %strong.text-info= company.name
        %td
          %span.badge.badge-primary= company.users.count
        %td= (company.contact.full_name if company.contact)
        %td= company.email
        %td.m-r
          = link_to company_path(company), type: 'button', class: 'btn btn-default btn-xs', data: { toggle: 'tooltip', placement: 'top', 'original-title' => 'Детали'} do
            %i.entypo-home
          = link_to company_path(company), type: 'button', class: 'btn btn-default btn-xs tooltip-primary', method: :delete, data: { confirm: 'Вы уверены?', toggle: 'tooltip', placement: 'top', 'original-title' => 'Удалить'} do
            %i.entypo-trash

