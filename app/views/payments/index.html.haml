%h3
  Платежи
%br
%table.pm-table.table.table-bordered
  %thead
    %tr
      %th ID транзакции
      %th Дата платежа
      %th Счет
      %th Клиент
      %th Сумма
      %th Метод
      %th Параметры
  %tbody
    - @payments.each do |payment|
      %tr
        %td
          = link_to payment_path(payment) do
            %strong.text-info= payment.trans
        %td= payment.payment_date
        %td
          = link_to invoice_path(payment.invoice) do
            %strong.text-info= payment.invoice.reference_no
        %td= payment.company.name
        %td= payment.amount
        %td= payment.payment_method.name
        %td
          .btn-group
            = button_tag class: 'btn btn-default btn-xs dropdown-toggle', data: {toggle: 'dropdown'} do
              Действия
              %span.caret
            %ul.dropdown-menu.dropdown-default{role: 'menu'}
              %li= link_to 'Просмотр', payment_path(payment)
              %li= link_to 'Изменить', edit_payment_path(payment), class: 'get-modal'
              %li= link_to 'Удалить', payment_path(payment), method: :delete




