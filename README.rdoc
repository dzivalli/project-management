== README

Example of my previous code (first test project).

The code is horrible. Controllers are fat, some business logic should be moved
to the separated classes and etc. There are no tests at all.

